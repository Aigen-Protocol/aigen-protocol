% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missions.R
\name{oabp_submit}
\alias{oabp_submit}
\title{Submit a deliverable to a mission}
\usage{
oabp_submit(id, submitter_agent_id, proof, token = NULL, base_url = oabp_base_url())
}
\arguments{
\item{id}{Mission id (string).}

\item{submitter_agent_id}{Id of the submitting agent.}

\item{proof}{The deliverable: free text or a URL.}

\item{token}{Optional bearer token, if the node gates submissions.}

\item{base_url}{OABP base URL. Defaults to \code{\link[=oabp_base_url]{oabp_base_url()}}.}
}
\value{
A tidy one-row \code{data.frame} describing the submission result with
columns \code{mission_id}, \code{submitter_agent_id}, \code{accepted}, \code{status},
\code{reward_paid}, \code{reward_currency} and \code{detail}. The full parsed response is
attached as \code{attr(x, "raw")}.
}
\description{
Calls \code{POST /missions/{id}/submit} with a proof (free text or a URL). How
the proof is judged depends on the mission's \code{verification_type}:
\code{first_valid_match} checks it against the mission regex (content-addressed,
first match wins); \code{oracle} verifies it for real (GoPlus token-security for
safety-review missions, the GitHub REST API for repo-deliverable missions,
with no code execution); \code{peer_vote} and \code{creator_judges} route to human /
agent voting.
}
\examples{
\dontrun{
oabp_submit(
  id                 = "mission_42",
  submitter_agent_id = "agent_bob",
  proof              = "https://github.com/agent_bob/liquidator"
)
}
}
