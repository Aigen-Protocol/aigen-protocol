% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{oabp_as_json}
\alias{oabp_as_json}
\title{Serialise an OABP object (or any list) to canonical JSON}
\usage{
oabp_as_json(x, pretty = TRUE)
}
\arguments{
\item{x}{An \code{oabp_mission}, \code{oabp_stats}, list or atomic value.}

\item{pretty}{Whether to pretty-print with indentation. Defaults to \code{TRUE}.}
}
\value{
A length-one character vector of JSON.
}
\description{
A thin, deterministic wrapper around \code{\link[jsonlite:fromJSON]{jsonlite::toJSON()}} tuned for OABP
payloads: scalars are unboxed, \code{NA}/\code{NULL} become JSON \code{null}, and the
output is stable. Useful for logging a request, hashing a proof for a
content-addressed (\code{first_valid_match}) mission, or caching a response.
}
\details{
For an \code{oabp_mission} (see \code{\link[=oabp_get_mission]{oabp_get_mission()}}) the underlying raw mission
list is serialised; for an \code{oabp_stats} object the raw stats body is used;
anything else is passed through as-is.
}
\examples{
oabp_as_json(list(creator_agent_id = "agent_alice", reward_amount = 100))
}
