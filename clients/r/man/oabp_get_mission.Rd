% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missions.R
\name{oabp_get_mission}
\alias{oabp_get_mission}
\title{Get one mission with its submissions and resolution}
\usage{
oabp_get_mission(id, base_url = oabp_base_url())
}
\arguments{
\item{id}{Mission id (string).}

\item{base_url}{OABP base URL. Defaults to \code{\link[=oabp_base_url]{oabp_base_url()}}.}
}
\value{
An \code{oabp_mission} object. Use \verb{$submissions} for the tidy
submissions frame, \verb{$resolution} for the raw resolution block and \verb{$raw}
for the unmodified parsed JSON.
}
\description{
Calls \code{GET /api/missions/{id}} and returns a structured \code{oabp_mission}
object carrying the mission metadata, a tidy \code{submissions} data.frame and
the \code{resolution} block (populated once a mission has been resolved and a
winner paid).
}
\examples{
\dontrun{
m <- oabp_get_mission("mission_42")
m                     # pretty-prints a summary
m$submissions         # tidy data.frame of deliverables
m$resolution          # who won, how much, fee
}
}
