% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{oabp_base_url}
\alias{oabp_base_url}
\title{Resolve the OABP base URL}
\usage{
oabp_base_url(url = NULL)
}
\arguments{
\item{url}{Optional explicit base URL. When \code{NULL} (the default) the
environment variable and built-in default are consulted.}
}
\value{
A length-one character vector: the resolved base URL with no
trailing slash.
}
\description{
Returns the base URL used for every request. Resolution order:
the explicit \code{url} argument, then the \code{OABP_BASE_URL} environment variable,
then the public default \code{https://cryptogenesis.duckdns.org}. A trailing
slash is always stripped so paths can be appended unambiguously.
}
\examples{
oabp_base_url()
oabp_base_url("https://node.example.com/")

# Honour an environment override:
old <- Sys.getenv("OABP_BASE_URL", unset = NA)
Sys.setenv(OABP_BASE_URL = "http://127.0.0.1:4025")
oabp_base_url()
if (is.na(old)) Sys.unsetenv("OABP_BASE_URL") else Sys.setenv(OABP_BASE_URL = old)
}
