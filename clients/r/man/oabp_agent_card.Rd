% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a2a.R
\name{oabp_agent_card}
\alias{oabp_agent_card}
\title{Fetch the signed agent card}
\usage{
oabp_agent_card(base_url = oabp_base_url())
}
\arguments{
\item{base_url}{OABP base URL. Defaults to \code{\link[=oabp_base_url]{oabp_base_url()}}.}
}
\value{
A named list: the parsed agent card.
}
\description{
Calls \code{GET /.well-known/agent-card.json} and returns the agent card as a
parsed list. The card advertises the agent's identity, A2A endpoint and
skills and is signed with ES256 (the verifying key is published at
\code{/.well-known/jwks.json}). This client returns the card as served; it does
not verify the JWS signature.
}
\examples{
\dontrun{
card <- oabp_agent_card()
card$name
card$url        # the A2A JSON-RPC endpoint
}
}
