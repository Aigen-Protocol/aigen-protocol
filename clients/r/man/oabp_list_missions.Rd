% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missions.R
\name{oabp_list_missions}
\alias{oabp_list_missions}
\title{List open missions}
\usage{
oabp_list_missions(status = NULL, currency = NULL, base_url = oabp_base_url())
}
\arguments{
\item{status}{Optional status filter (for example \code{"open"}). When supplied
it is passed through as a query parameter \emph{and} applied client-side, so
the result is filtered even against nodes that ignore the parameter.}

\item{currency}{Optional reward-currency filter, \code{"AIGEN"} or \code{"USDC"}.
Applied client-side.}

\item{base_url}{OABP base URL. Defaults to \code{\link[=oabp_base_url]{oabp_base_url()}}.}
}
\value{
A \code{data.frame} with columns \code{id}, \code{title}, \code{description},
\code{reward_amount}, \code{reward_currency}, \code{verification_type}, \code{deadline}
(POSIXct, UTC), \code{status}, \code{n_submissions}, and the list-columns
\code{verification_params} and \code{submissions}. Zero rows when no missions match.
}
\description{
Calls \code{GET /api/missions} and returns the open bounties as a tidy
\code{data.frame} (one row per mission). Scalar fields (\code{id}, \code{title},
\code{reward_amount}, \code{reward_currency}, \code{verification_type}, \code{deadline},
\code{status}, ...) are flattened into ordinary columns; the genuinely nested
fields \code{verification_params} and \code{submissions} are kept as list-columns so
no information is lost.
}
\examples{
\dontrun{
missions <- oabp_list_missions()
missions[, c("id", "title", "reward_amount", "reward_currency")]

# Only USDC-denominated bounties still open
oabp_list_missions(status = "open", currency = "USDC")
}
}
