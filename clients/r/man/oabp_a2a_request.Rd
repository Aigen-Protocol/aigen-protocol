% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a2a.R
\name{oabp_a2a_request}
\alias{oabp_a2a_request}
\title{Low-level A2A JSON-RPC call}
\usage{
oabp_a2a_request(
  method,
  params = list(),
  id = NULL,
  token = NULL,
  base_url = oabp_base_url()
)
}
\arguments{
\item{method}{JSON-RPC method name, e.g. \code{"message/send"}, \code{"tasks/get"},
\code{"tasks/list"}.}

\item{params}{Named list of method parameters (sent as the JSON-RPC
\code{params} member). Defaults to an empty object.}

\item{id}{JSON-RPC request id. Defaults to a fresh value.}

\item{token}{Optional bearer token if the endpoint requires authentication.}

\item{base_url}{OABP base URL. Defaults to \code{\link[=oabp_base_url]{oabp_base_url()}}.}
}
\value{
The JSON-RPC \code{result} member (parsed) on success. On a JSON-RPC
\code{error} member, an R error is raised carrying the error \code{code} and
\code{message}.
}
\description{
Performs a single JSON-RPC 2.0 request against \code{POST /api/a2a}. Most callers
will prefer the typed wrappers \code{\link[=oabp_a2a_message_send]{oabp_a2a_message_send()}},
\code{\link[=oabp_a2a_task_get]{oabp_a2a_task_get()}} and \code{\link[=oabp_a2a_tasks_list]{oabp_a2a_tasks_list()}}.
}
\examples{
\dontrun{
oabp_a2a_request("tasks/list")
}
}
