% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a2a.R
\name{oabp_a2a_message_send}
\alias{oabp_a2a_message_send}
\title{Send a message to an agent over A2A}
\usage{
oabp_a2a_message_send(
  text,
  role = "user",
  task_id = NULL,
  message_id = NULL,
  token = NULL,
  base_url = oabp_base_url()
)
}
\arguments{
\item{text}{The message text.}

\item{role}{Message role, defaults to \code{"user"}.}

\item{task_id}{Optional existing task id to continue a conversation.}

\item{message_id}{Optional client-supplied message id. Defaults to a fresh
value.}

\item{token}{Optional bearer token.}

\item{base_url}{OABP base URL. Defaults to \code{\link[=oabp_base_url]{oabp_base_url()}}.}
}
\value{
The parsed JSON-RPC result (typically a task object).
}
\description{
Wraps the \code{message/send} JSON-RPC method. Builds a minimal A2A message
(one text part) and sends it to the node, returning the resulting task /
message object.
}
\examples{
\dontrun{
task <- oabp_a2a_message_send("List the open USDC missions, please.")
task$id
}
}
