# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml

runtime: container

startCommand:
  type: http
  configSchema:
    type: object
    required: []
    properties: {}

build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
